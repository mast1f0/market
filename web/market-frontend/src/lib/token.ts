/** Совпадает с ключом в AuthContext / localStorage. */
export const ACCESS_TOKEN_KEY = "market_access_token";

/** Логин для отображения в профиле (сохраняется при входе). */
export const LOGIN_DISPLAY_KEY = "market_login";
